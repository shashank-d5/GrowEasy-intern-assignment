# Set this for AI-powered field mapping. Without it, the app uses built-in pattern matching.
# Get a free key at https://console.groq.com/keys (no credit card needed)
GROQ_API_KEY=your_groq_api_key_here
